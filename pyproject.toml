[project]
name = "acbb-kayak-sup"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "fmeurou", email = "frederic@meurou.fr" }
]
requires-python = ">=3.11"
dependencies = [
    "markdown>=3.10.3",
    "pelican>=4.12.0",
]

[project.scripts]
acbb-kayak-sup = "acbb_kayak_sup:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
